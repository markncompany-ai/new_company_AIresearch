# 기술적 제약사항 — 개발팀 전달용

기업 정보 자동 수집 시 부딪히는 제약을 정리했다.
**개발로 해결 가능한 것(A)** 과 **프로세스 문제(B)** 를 구분했다. 섞으면 엔지니어링으로 못 푸는 걸 요청하게 된다.

---

## A. 기술적 제약

### A-1. 자바스크립트로 그려지는 홈페이지는 내용을 못 읽음 ★최우선

**증상**: `stradvision.com`을 가져오면 본문이 **완전히 빈 상태**로 나옴

**원인**: 서버가 준 원본 HTML만 읽고 자바스크립트를 실행하지 않음.
React/Vue 등으로 만든 사이트는 내용이 JS로 그려져 원본 HTML이 비어 있음

**영향 항목**: 사업자번호, 대표 연락처, 대표 이메일, 로고 주소, 서비스 URL
(전부 푸터·Contact 페이지에 있는 것들)

**해결**: 헤드리스 브라우저(Playwright)로 실제 렌더링 후 텍스트 추출
→ **`03_스크래퍼/`에 구현 완료.** 로컬 픽스처 검증 통과(287바이트 → 전체 추출), 실제 사이트 E2E는 미실시

---

### A-2. 이미지 파일 자체를 못 읽음

**증상**: 로고 이미지를 가져오려 하면 "Image content is not supported"

**영향**: 로고의 실제 크기·화질을 미리 확인 불가.
피지오로보틱스 로고가 57×57인 걸 파일명(`cleaned_logo_57x57.png`)으로 추측했을 뿐,
파일명에 크기가 없으면 알 수 없음

**추가 사례**: 플레이타운은 **사업자등록번호를 이미지로** 올려둠(`사업자등록번호.png`) → 텍스트 추출 불가

**해결**: 이미지 다운로드 후 크기 판별 → **스크래퍼에 구현 완료(PIL 실측)**
이미지 속 텍스트는 OCR 필요(미구현)

---

### A-3. 일반 웹 접근 차단 (작업 환경 제약)

**증상**: 로고 다운로드·URL 유효성 검사 시 403 (`host_not_allowed`)

**영향**: URL이 실제로 살아있는지 검증 불가

**해결**: 개발팀 서버에서 HTTP 상태 확인 → **스크래퍼에 `check_url_alive()` 구현 완료**

---

### A-4. 검색 결과는 요약본이라 푸터 정보가 안 나옴

**증상**: 검색으로는 사업자번호·이메일·연락처가 거의 안 잡힘

**원인**: 검색 엔진은 페이지 상단 본문 위주로 요약. 푸터는 요약에 포함되지 않음

**해결**: 홈페이지 직접 방문 필수 (A-1과 연결)

---

### A-5. 방문 가능한 URL에 제약

검색 결과에 나오지 않은 URL은 바로 가져올 수 없어 우회가 필요. 조사 단계가 늘어남

---

### A-6. 대표자 SNS 본인 확인 불가 ★개발로도 해결 어려움

**증상**: 김준환(스트라드비젼 대표) 링크드인 검색 시 **동명이인만** 나옴
(스카우트파트너스 전무, SBS 팀장 등) — 실제 대표 프로필은 특정 불가

**원인**: 링크드인은 로그인 없이 프로필 접근이 대부분 차단됨. 한국 이름은 동명이인이 많음

**영향**: 자동 수집 신뢰도가 구조적으로 낮음. 잘못 넣으면 심각한 오류

**권고**: **처음부터 "사람이 확인하는 항목"으로 설계할 것**

---

### A-7. 공식 기업정보 DB 접근 없음

**증상**: 사업자번호를 공적 출처에서 확인 불가. 홈페이지에 적힌 것만 사용

**영향**: 홈페이지에 사업자번호가 없으면 수집 불가

**해결**: 국세청 사업자등록정보 API 또는 NICE 데이터 연동

---

### A-8. 연락처가 하위 페이지에 있음

**증상**: 메인 푸터엔 사업자번호만 있고, 전화·이메일은 `/contact`에 있는 경우가 많음
(플레이타운은 이메일이 `/business`에 있었음)

**해결**: 하위 페이지 자동 탐색 → **스크래퍼에 구현 완료** (contact/about/products 순회)

---

## B. 프로세스 문제 (기술 아님)

개발로 풀 문제가 아니라 조사 규칙을 지켜야 하는 부분. 실제로 발생했던 것들.

| 문제 | 실제 사례 | 규칙 |
|---|---|---|
| URL 추측 생성 | `stradvision.com/svnet/`, 로고 주소 조합 → 둘 다 가짜 | 검색·페이지에서 **실제 본 URL만** |
| 조사 절차 불일치 | 한 곳은 홈페이지 방문, 다른 곳은 검색만 → 푸터 정보 누락 | **모든 기업 홈페이지 직접 방문** |
| 뉴스 검색 부족 | 뉴스가 있는데 "없음"으로 처리 | 기업명 검색 + **홈페이지 뉴스 페이지까지** |
| 사명 표기 미확인 | 스트라드비전 vs 스트라드비젼(실제 등록명) | 표기 차이 발견 시 **즉시 보고** |
| 키워드 임의 생성 | "인공지능", "재활" 등 목록에 없는 이름 | **284개 목록에서만** 선택 |

---

## C. 항목별 자동수집 가능성

| 항목 | 수집도 | 주요 제약 |
|---|---|---|
| 홈페이지·로고 | 중 | JS사이트(A-1), 화질 사전확인(A-2) — 스크래퍼로 해결됨 |
| 사업자번호 | 중 | 푸터 의존(A-1), 이미지인 경우 불가(A-2), 미기재 시 불가(A-7) |
| 대표자명 | 상 | 대체로 확보 가능 |
| **대표자 SNS** | **하** | 동명이인·접근차단(A-6) → **사람 확인 권장** |
| 소개글(양식) | 상 | 업계 정확 용어는 검수 필요 |
| 영문소개 | — | 번역기 아티팩트로 처리 |
| 카테고리 | 상 | [참고]소개 기반 자동 추천 |
| 키워드 | 상 | 284개 목록 내에서만 |
| 연락처 | 중 | 푸터 의존(A-1, A-8). 010이면 규칙상 제외 |
| 이메일 | 중 | 푸터 의존(A-1, A-8). 개인메일이면 규칙상 제외 |
| 서비스 | 중 | B2B는 애초에 없음. 동명 앱 주의 |
| 뉴스 | 상 | 언론사·발행일 메타데이터 의존 |

---

## D. 요청 우선순위

1. **헤드리스 브라우저 렌더링** (A-1) — 구현 완료, **실제 사이트 E2E 검증 필요**
   → 사업자번호·연락처·이메일·로고 수집률이 한 번에 올라감. 가장 효과 큼
2. **URL 유효성 검사** (A-3) — 구현 완료
3. **로고 크기 판별** (A-2) — 구현 완료
4. **검색 API 검토** — 기업 식별·뉴스 수집 보강
   뉴스는 제목/URL/언론사/보도일이 필요하니 **메타데이터가 구조화돼 나오는지 먼저 확인**
5. **사업자등록정보 API** (A-7)
6. **OCR** — 이미지로 된 사업자번호 (A-2 잔여)

**대표자 SNS(A-6)는 개발로도 해결이 어려우니 사람 확인 항목으로 두는 것을 권함.**
