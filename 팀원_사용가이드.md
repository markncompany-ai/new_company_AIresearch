# 기업 등록 1차 조사 — 팀원 사용 가이드

혁신의숲 신규 기업 등록 1차 조사를 Claude로 수행하는 방법입니다.
사용 환경에 따라 두 가지 방법 중 하나를 쓰면 됩니다.

---

## 방법 A. 클로드코드(Claude Code)를 쓰는 경우 (권장)

### 처음 1회 설정
1. 공유받은 폴더에서 아래 2개를 자기 PC의 작업 폴더에 복사한다.
   - `.claude/skills/company-research/SKILL.md` → 작업 폴더의 같은 경로에
   - `기업등록/` 폴더 전체 (검수작업대.html 포함)
2. 그 작업 폴더에서 클로드코드를 연다.

### 사용법
채팅창에 이렇게 입력:

```
/company-research 기업명 / 사업자번호 / 대표자명
```

- 사업자번호·대표자명은 없으면 생략 가능 (있으면 정확도가 크게 올라감)
- 여러 기업이면 줄바꿈으로 이어서 입력
- 그냥 "OO기업 조사해줘"라고 해도 스킬이 실행됨

### 결과 확인
- 채팅에 JSON이 출력되고, `기업등록/결과/` 폴더에도 파일로 저장됨
- JSON 아래의 **보고 사항**(못 찾은 항목, 유사 사명 주의, 검수 필요 표기)을 꼭 읽을 것

### 클로드코드의 장점
- 실제 브라우저로 홈페이지를 직접 열어 확인함 → JS로 그려지는 사이트(스트라드비젼 등)도 푸터를 읽을 수 있음
- 결과가 파일로 남아서 나중에 찾기 쉬움

## 방법 B. 웹 클로드(claude.ai)를 쓰는 경우

1. `표준지시문_원본_웹클로드용.md` 파일을 연다
2. **전체를 복사**해서 클로드 대화창에 붙여넣는다
3. 맨 아래 "조사할 기업 목록"에 기업명 / 사업자번호 / 대표자명을 적어 보낸다
4. 출력된 JSON을 복사한다

⚠️ 웹 클로드는 JS 렌더링 사이트의 푸터를 못 읽는 경우가 있음.
"홈페이지를 읽을 수 없었다"는 보고가 나오면 해당 항목은 직접 확인할 것.

---

## 공통: 검수 (두 방법 모두 동일)

1. `검수작업대.html`을 더블클릭해서 브라우저로 연다
2. 위쪽 입력창에 JSON을 붙여넣고 추가한다
3. 기업별 카드에서 색상 우선순위로 검수한다:
   - **초록(확실)**: 사업자번호 대조 등 근거 확실 → 그대로 사용
   - **노랑(확인권장)**: 근거 있으나 확인 필요 → 훑어보기
   - **빨강(확인필요)**: 근거 부족·규칙 위반 → 반드시 직접 확인
4. 영문 소개는 번역기, 로고는 256px 다운로드로 보조 처리
5. 어드민에 입력 (복사 버튼 / 뉴스는 일괄복사 → 엑셀 양식)

## 검수할 때 특히 조심할 것

- **대표자 SNS**: 동명이인 위험이 가장 큰 항목. 자동 수집을 신뢰하지 말고 사람이 확인
- **"검수 필요" 표기된 이메일**: 개인 도메인(gmail 등)인데 회사 계정으로 보이는 경우. 사유를 읽고 판단
- **유사 사명**: 비슷한 이름의 다른 회사가 아닌지 대표자명·사업자번호로 교차 확인
- **비어 있는 항목**: 클로드가 못 찾은 것. 억지로 채워진 것보다 안전하니, 필요하면 직접 조사해서 채울 것

## 규칙을 바꾸고 싶을 때

조사 규칙(이메일 판정, 키워드 목록 등)을 바꾸려면 **4곳을 함께** 고쳐야 합니다.
운영매니저에게 요청해 주세요.
1. 클로드코드 스킬 (`.claude/skills/company-research/SKILL.md`)
2. 웹 클로드용 원본 지시문 (`표준지시문_원본_웹클로드용.md`)
3. 검수작업대 HTML
4. 개발팀 스크래퍼
